import { EntityType } from '../models';

export const ValidInfoEntities: EntityType[] = [
  EntityType.Person,
  EntityType.Horse,
  EntityType.Team,
  EntityType.Competition,
  EntityType.Event,
  EntityType.Venue,
  EntityType.Organization,
];
